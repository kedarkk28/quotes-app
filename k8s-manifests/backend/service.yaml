apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: quotes-app
  labels:
    app: backend
    tier: api
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30083
  type: NodePort